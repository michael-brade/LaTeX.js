{
  "extends": "../tsconfig.json",

  "compilerOptions": {
    "types": ["mocha", "chai", "node"],
    "esModuleInterop": true,
    "resolveJsonModule": true,

    // test/ is usually not compiled, but don't clutter the directory by accident
    "outDir": "tmp"
  },

  "include": [
    "**/*.ts"
  ]
}