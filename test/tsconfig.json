{
  "extends": "../tsconfig.json",

  "compilerOptions": {
    "types": ["mocha", "chai", "node"],
    "esModuleInterop": true,
    "resolveJsonModule": true,

    // test has no need for transpiling
    "noEmit": true
  },

  "include": [
    "**/*.ts"
  ]
}